"""MCP tools module.

Tool implementations live in ``simple_tools`` (single-app 3-tool server) and
``multi_app_tools`` (multi-app 7-tool progressive disclosure). The earlier
per-operation tool modules were consolidated into those two and removed.
"""
